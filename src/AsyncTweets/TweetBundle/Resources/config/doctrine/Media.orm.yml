# generated by the following command:
# php app/console generate:doctrine:entity \
#  --entity=AsyncTweetsTweetBundle:Media \
#  --fields="created_at:datetime text:string(255) retweet_count:integer \
#  favorite_count:integer" --format=yml

AsyncTweets\TweetBundle\Entity\Media:
    type: entity
    table: media
    id:
        id:
            type: bigint
            id: true
    fields:
        media_url_https:
            type: string
            length: '255'
        url:
            type: string
            length: '255'
        display_url:
            type: string
            length: '255'
        expanded_url:
            type: string
            length: '255'
    manyToMany:
        tweets:
            targetEntity: Tweet
            mappedBy: medias
    lifecycleCallbacks: {  }
